@{
    ViewBag.Title = "About";
}
<h2>@ViewBag.Title.</h2>
<h3>Your Current Application Roles:</h3>
<ul>
    @foreach (string str in (List<string>)ViewData["myroles"])
    {
        <li>@str</li>
    }
</ul>
<h3>Your Current Groups:</h3>
<ul>
    @foreach (string str in (List<string>)ViewData["mygroups"])
    {
        <li>@str</li>
    }
</ul>